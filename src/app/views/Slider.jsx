import React from 'react'
import RTSlider from 'react-toolbox/lib/slider'
import theme from 'Styles/Slider.scss'

const Slider = (props) => (
  <RTSlider theme={theme} {...props} />
)

export default Slider
